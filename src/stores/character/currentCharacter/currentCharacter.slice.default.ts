import { CurrentCharacterSliceData } from "./currentCharacter.slice.type";

export const defaultCurrentCharacterSlice: CurrentCharacterSliceData = {
  currentCharacterId: undefined,
  currentCharacter: undefined,
};
